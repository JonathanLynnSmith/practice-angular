{
  "name": "poc-angular-config",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "npm run copy:config init && ng serve",
    "start:local": "npm run copy:config local && ng serve",
    "start:dev": "npm run copy:config dev && ng serve",
    "start:beta": "npm run copy:config beta && ng serve",
    "start:azure": "npm run copy:config azure && ng serve",
    "start:test": "npm run copy:config test && ng serve",
    "start:training": "npm run copy:config training && ng serve",
    "start:prod": "npm run copy:config prod && ng serve",
    "copy:config": "node src/scripts/copy-config.js",
    "build": "ng build",
    "build:local": "npm run copy:config local && ng build",
    "build:dev": "npm run copy:config dev && ng build",
    "build:beta": "npm run copy:config beta && ng build",
    "build:azure": "npm run copy:config azure && ng build",
    "build:test": "npm run copy:config test && ng build",
    "build:training": "npm run copy:config training && ng build",
    "build:prod": "npm run copy:config prod && ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true,
  "dependencies": {
    "@angular/animations": "^15.0.0",
    "@angular/common": "^15.0.0",
    "@angular/compiler": "^15.0.0",
    "@angular/core": "^15.0.0",
    "@angular/forms": "^15.0.0",
    "@angular/platform-browser": "^15.0.0",
    "@angular/platform-browser-dynamic": "^15.0.0",
    "@angular/router": "^15.0.0",
    "fs-extra": "^11.1.1",
    "rxjs": "~7.5.0",
    "tslib": "^2.3.0",
    "zone.js": "~0.12.0"
  },
  "devDependencies": {
    "@angular-devkit/build-angular": "^15.0.4",
    "@angular/cli": "~15.0.4",
    "@angular/compiler-cli": "^15.0.0",
    "@types/jasmine": "~4.3.0",
    "jasmine-core": "~4.5.0",
    "karma": "~6.4.0",
    "karma-chrome-launcher": "~3.1.0",
    "karma-coverage": "~2.2.0",
    "karma-jasmine": "~5.1.0",
    "karma-jasmine-html-reporter": "~2.0.0",
    "typescript": "~4.8.2"
  }
}
